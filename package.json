{
    "name": "lintbase-root",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build --workspaces --if-present",
        "test": "npm run test --workspaces --if-present",
        "dev:cli": "npm run dev --workspace=packages/lintbase",
        "dev:mcp": "npm run dev --workspace=packages/lintbase-mcp"
    }
}