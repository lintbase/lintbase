# Security Rules Context

This file defines the basic security boundaries and rules for the database collections.
Agents writing client-side queries must ensure they align with these rules.

## Known Collections
- `Baux`: Default rules apply unless specifically overridden.
- `Immeubles`: Default rules apply unless specifically overridden.
- `Leads`: Default rules apply unless specifically overridden.
- `Meetings`: Default rules apply unless specifically overridden.
- `Membres`: Default rules apply unless specifically overridden.
- `NewLease`: Default rules apply unless specifically overridden.
- `NewTenant`: Default rules apply unless specifically overridden.
- `NewTicket`: Default rules apply unless specifically overridden.
- `StakeHolders`: Default rules apply unless specifically overridden.
- `Users`: Default rules apply unless specifically overridden.
- `bankinfo`: Default rules apply unless specifically overridden.
- `consoleLog`: Default rules apply unless specifically overridden.
- `infoskokote`: Default rules apply unless specifically overridden.
- `monthlyreport`: Default rules apply unless specifically overridden.
- `pendingrewal`: Default rules apply unless specifically overridden.
- `releve`: Default rules apply unless specifically overridden.
- `request`: Default rules apply unless specifically overridden.
- `requestGet`: Default rules apply unless specifically overridden.
- `requestPost`: Default rules apply unless specifically overridden.
- `stake`: Default rules apply unless specifically overridden.
- `supportForm`: Default rules apply unless specifically overridden.
- `surveySatisfaction`: Default rules apply unless specifically overridden.
- `testPayload`: Default rules apply unless specifically overridden.

> Note: This is an auto-generated baseline based on discovered collections. Verify explicit security scopes in the actual `firestore.rules` or equivalent file.